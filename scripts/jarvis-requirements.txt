# Voice mode (scripts/jarvis.py). Installed into scripts/.jarvis-venv by
# scripts/jarvis-setup.sh — not part of the Node project.
openwakeword>=0.6
onnxruntime>=1.17          # openwakeword inference backend (macOS arm64 wheels)
sounddevice>=0.4.6         # mic capture (PortAudio; ships wheels)
faster-whisper>=1.2.1
slack-sdk>=3.43
numpy>=1.26
# VAD: original webrtcvad is sdist-only/unmaintained; the -wheels fork ships
# macOS arm64 wheels (import name is still `webrtcvad`). jarvis.py falls back
# to RMS silence detection if this is missing.
webrtcvad-wheels>=2.0.14
