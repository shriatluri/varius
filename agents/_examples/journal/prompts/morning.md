Produce this morning's message. Follow the "Scheduled: the 7:00 AM run"
section of CLAUDE.md exactly: read `TODO.md` and recent `entries/`, curate
and rewrite `TODO.md`, then return the morning to-do message in the
specified output format. If today is Sunday, include the weekly analysis
(last 7 days of `entries/`) before the list.
